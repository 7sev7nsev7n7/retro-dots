general {
  ignore_empty_input = true
}

animations {
  animation = fadeIn, on, 2, linear
  animation = fadeOut, on, 2, linear
}

background {
  path = ~/media/pictures/wallpapers/nixos
  color = rgba(0,0,0,0)
  blur_passes = 3
  blur_size = 3
  brightness = 1
}

label {
  shadow_passes = 4
  shadow_boost = 4

  monitor =
  text = cmd[update:1000] date -Iseconds
  text_align = center
  font_size = 32
  font_family = Iosevka
  position = 0, 32
}

input-field {
  monitor =
  size = 300, 60
  outline_thickness = 2
  font_family = Iosevka
  fade_on_empty = true
  fade_timeout = 1000
  placeholder_text = 
  rounding = 8
  fail_color = rgb(255,0,0)
  fail_text = 
  position = 0, 32
  valign = bottom
}
